<!-- Fallback link to the menu, replaced on page load with an iframe version
     -->
<div id="plone-toolbar-trigger">

    <script type="text/javascript">
        $(document).ready(function() {
            if ($('body').hasClass('template-view') &&
                $('body').hasClass('portaltype-page')) {
                $('#toolbar-button-edit', $('#plone-toolbar').contents())
                    .click(function() {
                        e.preventDefault();
                        e.stopPropagation();
                        $.deco.panels('*').activate();});}});
    </script>

    <style type="text/css">
    /*
     * TODO: this is here only temporary
     */
    #visual-portal-wrapper {
        position: relative;
        top: 46px;
    }
    iframe#plone-toolbar {
        margin: 0;
        padding: 0;
        border: 0;
        outline: 0;
    
        width: 100%;
        height: 45px;
        z-index: 500;
    
        background-color: transparent;
        position: fixed;
        left: 0;
        top: 0;
    
        /*
        position: absolute;
        top: expression(eval((document.body.scrollTop)?document.body.scrollTop:document.documentElement.scrollTop));
        -webkit-box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        -moz-box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        (/
    }
    
    </style>

    <iframe
       src="@@plone_toolbar"
       id="plone-toolbar"
       name="plone-toolbar"
       klass="plone-toolbar"
       tal:attributes="src string:${context/absolute_url}/@@plone_toolbar">
      <a id="plone-toolbar-link" href="@@plone_toolbar" target="_blank"
         tal:attributes="href string:${context/absolute_url}/@@plone_toolbar"
         i18n:translate="">
        Manage page
      </a>
    </iframe>

</div>
